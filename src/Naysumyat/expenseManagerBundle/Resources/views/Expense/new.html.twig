{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Expense creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('expense') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
